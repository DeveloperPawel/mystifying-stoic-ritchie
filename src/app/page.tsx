import React, { useEffect } from "react";
import MainHome from "@/components/main-home";

export const metadata = {
  title: "ROA - PWA Mobile Next js Template",
};

const index = () => {
  return <MainHome />;
};

export default index;
